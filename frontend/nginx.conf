server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA : toute route inconnue retombe sur index.html (React Router côté client)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
